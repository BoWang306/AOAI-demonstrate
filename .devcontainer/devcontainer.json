{
	"name": "Azure OpenAI 模型测试门户",
	
	// 使用 Docker Compose
	"dockerComposeFile": "docker-compose.yml",
	"service": "app",
	"workspaceFolder": "/workspace",
	
	// 配置特定功能
	"features": {
		"ghcr.io/devcontainers/features/git:1": {},
		"ghcr.io/devcontainers/features/github-cli:1": {}
	},

	// 容器创建后运行的命令
	"postCreateCommand": "echo '📦 安装依赖...' && pip install --upgrade pip && pip install -r requirements.txt",

	// 自动安装的 VS Code 扩展
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance",
				"ms-python.black-formatter",
				"ms-toolsai.jupyter",
				"redhat.vscode-yaml",
				"eamodio.gitlens",
				"GitHub.copilot",
				"streetsidesoftware.code-spell-checker",
				"ms-azuretools.vscode-docker"
			],
			"settings": {
				"python.defaultInterpreterPath": "/usr/local/bin/python",
				"python.linting.enabled": true,
				"python.linting.pylintEnabled": false,
				"python.linting.flake8Enabled": true,
				"python.formatting.provider": "black",
				"editor.formatOnSave": true,
				"editor.codeActionsOnSave": {
					"source.organizeImports": "explicit"
				},
				"files.exclude": {
					"**/__pycache__": true,
					"**/*.pyc": true
				},
				"terminal.integrated.defaultProfile.linux": "bash"
			}
		}
	},

	// 端口转发配置
	"forwardPorts": [8501],
	"portsAttributes": {
		"8501": {
			"label": "Streamlit App",
			"onAutoForward": "notify",
			"protocol": "http"
		}
	},

	// 容器启动后自动运行
	"postStartCommand": "echo '🚀 Azure OpenAI 测试门户已准备就绪！' && echo '💡 运行 streamlit run app.py 启动应用' && echo '📖 查看 .devcontainer/README.md 了解更多信息'",

	// 容器用户
	"remoteUser": "vscode",

	// 环境变量
	"remoteEnv": {
		"PYTHONUNBUFFERED": "1"
	}
}
